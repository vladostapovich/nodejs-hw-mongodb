description: Conflict, email already exists.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: A user with this email already exists.
