description: Bad request, invalid input data.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Invalid input data. Please check the request body.
